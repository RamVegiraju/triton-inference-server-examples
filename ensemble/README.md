# Ensemble Workflow Example
NOTICE: Ensure to create a .pt file yourself here, the artifact via git lfs may or may not work that's in the repo, you can create a pt file as shown in NB and move it to where existing pt file in repo is (delete the original if cloning repo).
